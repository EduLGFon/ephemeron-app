/// Built-in cross-list filtered views. Custom smart lists (e.g. user-
/// defined "high priority" saved filters) are a later phase — this
/// covers the fixed set named in the MVP cut.
enum SmartListType { today, tomorrow, next7Days, completed, trash, wontDo }
